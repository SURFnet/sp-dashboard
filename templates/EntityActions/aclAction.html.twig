{% if action.allowAclAction %}
    <li>
        <a href="{{ path('entity_acl_idps', {id: action.id, serviceId: action.serviceId}) }}">
            <i class="fa fa-list-ul" aria-hidden="true"></i>
            {{ 'entity.list.action.acl'|trans }}
        </a>
    </li>
{% endif %}
