imports:
    # Load the test services.yml overload file
    - { resource: services.yml }

framework:
    test: true
    session:
        storage_factory_id: surfnet.dashboard.session.storage.mock_file
    profiler:
        collect: false
